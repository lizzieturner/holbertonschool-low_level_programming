#include <stdio.h>

/**
 * main - entry point for c, containing other functions within
 * Return: 0 if program successfully completes
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
